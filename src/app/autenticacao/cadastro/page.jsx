import React from 'react'

const Cadastro = () => {

    // Fluxo para cadastro
    
    // Primerio faz uma chamada API do tipo POST para cadastrar o usuario
    
    // Em seguida, segue-se o fluxo de autenticação de login com:
    
    // const { signIn } = useContext(AuthContext)

    // signIn()

    return (
        <div>Cadastro</div>
    )
}

export default Cadastro